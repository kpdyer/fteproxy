# Tests depend on repository-only examples and stay out of distributions.
prune fteproxy/tests

# Design docs are not package data, so setuptools will not include them on its
# own. Runtime definitions are included by [tool.setuptools.package-data].
include SECURITY.md
include PERFORMANCE.md
include docs/*.md
