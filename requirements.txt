# libfte 0.4.x is not on PyPI yet (PyPI has fte up to 0.3.0), so install the
# new fte.FTE engine straight from git. This tracks the perf/aes-ctr-hmac branch
# (libfte PR #92): O(n) rank_offset (#89, merged) plus the OpenSSL
# AES-CTR+HMAC-SHA256 cipher, so the formatted header uses the same construction
# as the hybrid body here. Both are coordinated drafts; pin a commit or move to
# the PyPI specifier "fte>=0.4.0" once #92 lands and 0.4 ships.
fte @ git+https://github.com/kpdyer/libfte.git@perf/aes-ctr-hmac
cryptography>=41.0
pytest>=9.1.1
pytest-timeout>=2.4.0
